# 🎮 Rock Paper Scissors — Reinforcement Learning Game

**ML Semester Project · Group: Arshad Anwar 425 · Behram Ahmad Khan 480 · Ahsan Javed 438**

A Human vs AI Rock Paper Scissors game powered by a Q-Learning agent, with a FastAPI backend and a pure-HTML/CSS/JS frontend — fully deployable to the web.

---

## 📁 Project Structure

```
rps-rl-game/
│
├── backend/
│   ├── agent.py          ← Q-Learning agent (all RL logic)
│   ├── main.py           ← FastAPI app (REST API + serves frontend)
│   └── requirements.txt  ← Python deps (backend only)
│
├── frontend/
│   ├── index.html        ← Main UI page
│   ├── css/
│   │   └── style.css     ← All styles
│   └── js/
│       ├── api.js        ← Fetch wrapper for the backend
│       └── game.js       ← UI logic, chart, Q-table rendering
│
├── run.py                ← One-command local server
├── requirements.txt      ← Python deps (root, for deployment)
├── Procfile              ← Railway / Heroku
├── railway.json          ← Railway.app config
├── render.yaml           ← Render.com config
├── Dockerfile            ← Docker / any cloud
└── .gitignore
```

---

## ⚙️ Tech Stack

| Layer      | Technology            |
|------------|-----------------------|
| Backend    | Python 3.12, FastAPI  |
| RL Logic   | Pure Python (Q-Learning) |
| Frontend   | HTML5 + CSS3 + Vanilla JS |
| Chart      | Chart.js (CDN)        |
| Server     | Uvicorn (ASGI)        |
| Deployment | Railway               |

---

## 🧠 Reinforcement Learning Details

### Algorithm: Q-Learning

**Bellman Update Equation:**
```
Q(s,a) ← Q(s,a) + α [ r + γ · max_a' Q(s', a') − Q(s,a) ]
```

| Symbol | Meaning           | Value |
|--------|-------------------|-------|
| α      | Learning rate     | 0.30  |
| γ      | Discount factor   | 0.90  |
| ε      | Explore rate      | 1.0 → 0.05 |
| ε decay| Per episode       | 0.995 |

### State Space
State = `(AI's last move, Human's last move)` → 10 states (9 pairs + "Start")

### Action Space
`{0: Rock, 1: Paper, 2: Scissors}` → 3 actions

### Reward Function
| Outcome | Reward |
|---------|--------|
| AI Wins | +1.0   |
| Draw    |  0.0   |
| AI Loses| −1.0   |

### Policy
- **ε-greedy exploration**: random action with prob ε, else greedy
- ε decays 0.5% per episode until minimum of 0.05
- After training, agent exploits learned Q-values

---

## 🚀 Local Setup (Step by Step)

### Step 1 — Clone / download the project
```bash
git clone https://github.com/YOUR_USERNAME/rps-rl-game.git
cd rps-rl-game
```

### Step 2 — Create a Python virtual environment
```bash
# Windows
python -m venv venv
venv\Scripts\activate

# macOS / Linux
python3 -m venv venv
source venv/bin/activate
```

### Step 3 — Install dependencies
```bash
pip install -r requirements.txt
```

### Step 4 — Run the server
```bash
python run.py
```

### Step 5 — Open in browser
```
http://localhost:8000
```

That's it! The FastAPI server serves both the API and the HTML frontend.

---

## 🌐 API Endpoints

| Method | Endpoint          | Description                          |
|--------|-------------------|--------------------------------------|
| GET    | `/health`         | Health check + episode count         |
| POST   | `/play`           | Human plays one round                |
| POST   | `/train`          | Auto-train agent N episodes          |
| GET    | `/stats`          | Full agent statistics                |
| GET    | `/qtable`         | Live Q-table (all 10 states)         |
| GET    | `/reward-history` | Episode reward data for chart        |
| POST   | `/reset`          | Reset agent + delete saved state     |
| POST   | `/save`           | Save agent to `agent_state.json`     |
| POST   | `/load`           | Load agent from `agent_state.json`   |

**Interactive API docs:** http://localhost:8000/docs (Swagger UI, auto-generated by FastAPI)


## 📊 How to Generate Training Graphs (for Report)

1. Open the app at `http://localhost:8000`
2. Click **Train 200×** three times (600 total episodes)
3. Screenshot the "Reward vs Episode" chart — it shows the agent converging
4. The Q-Table section shows the learned policy in real time
5. For more data: click Train 200× more times and watch the chart flatten near +0.3


## 👥 Group Members

| Name              | Roll No |
|-------------------|---------|
| Arshad Anwar      | 425     |
| Behram Ahmad Khan | 480     |
| Ahsan Javed       | 438     |

